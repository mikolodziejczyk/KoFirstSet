import { MyClass } from "./myclass";
import { AnotherClass } from "./anotherclass";

describe("The second suite", function () {
    // var a;

    // it("A passing test", function () {
    //     a = false;

    //     expect(a).not.toBe(true);
    // });

    // it("Constructor should return an instance", function () {
    //     let instance = new AnotherClass();

    //     expect(instance).toBeTruthy();
    // });

    // it("Equals a numeric value", function () {
    //     let a: number = 12;

    //     expect(a).toEqual(12);
    //     expect(a).not.toEqual(13);
    //     expect(a).toBeGreaterThan(11);
    //     expect(a).toBeGreaterThanOrEqual(12);
    //     expect(a).toBeLessThan(13);
    //     expect(a).toBeLessThanOrEqual(12);
    // });

    // it("Equals a string value", function () {
    //     let a: string = "hello";

    //     expect(a).toEqual("hello");

    // });

    // it("Equals object", function () {
    //     let a = { myProperty: "test" };
    //     let b = JSON.parse("{ \"myProperty\" : \"test\"}");

    //     expect(a).toEqual(b);
    // });

    // it("Matches", function () {
    //     let a = "Hello!"

    //     expect(a).toMatch(/hello/i);
    //     expect(a).not.toMatch(/^xxx/i);
    // });

    // it("Matches", function () {
    //     let a = "test";
    //     let b = undefined;
    //     let c = null;

    //     expect(a).toBeDefined();
    //     expect(b).toBeUndefined();
    //     expect(c).toBeNull();
    //     expect(b).not.toBeNull();
    // });

    // it("Contain", function () {
    //     let ar = ["Tom", "Dick", "Harry"];

    //     expect(ar).toContain("Harry");
    //     expect(ar).not.toContain("John");
    // });

    // it("Fails", function () {
    //     // fail("This test fails.");
    // });


    // it("Async test", function (done) {
    //     window.setTimeout(() => {
    //         expect(true).toBe(true);
    //         done();
    //     }, 1000);
    // });

    // it("Promise resolve test", function (done) {
    //     let p = AnotherClass.getPromise("Hello", false);
    //     p.then((s: string) => {
    //         expect(s).toBe("Hello");
    //         done();
    //     });
    //     p.catch(() => { fail("The promise should not fail."); });
    // });

    // it("Promise fail test", function (done) {
    //     let p = AnotherClass.getPromise("Hello", true);
    //     p.then((s: string) => { fail("The promise should not resolve."); });
    //     p.catch(() => { done(); });
    // });
});